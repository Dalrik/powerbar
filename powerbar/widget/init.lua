return
{
    bat = require("powerbar.widget.bat");
    clock = require("powerbar.widget.clock");
    systray = require("powerbar.widget.systray");
    taglist = require("powerbar.widget.taglist");
    prompt = require("powerbar.widget.prompt");
}
